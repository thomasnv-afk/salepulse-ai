name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Deployed to Vercel
        run: echo "✅ Build deploying to Vercel automatically"
